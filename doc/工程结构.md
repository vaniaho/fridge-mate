三人团队（A负责图像、B负责语音、C负责云端大模型及本地逻辑和系统），采用 ESP-IDF 的"组件化（Components）"设计模式。

***

## 一、 项目目录树

基于 ESP-IDF 构建工具 (CMake)，项目根目录如下：

```text
smart_fridge_project/
├── CMakeLists.txt                # 全局构建脚本
├── sdkconfig.defaults            # 全局系统默认配置 (如开启 PSRAM、设置 CPU 频率等)
├── partitions.csv                # 自定义分区表 (factory 4M + model 3M + storage 12M)
│
├── main/                         # 主程序入口与系统总调度
│   ├── CMakeLists.txt
│   ├── main.cpp                  # app_main 入口 (包裹在 extern "C" 中)
│   ├── system_events.h           # ★ 核心：定义全局事件枚举和结构体，三人共用
│   └── task_manager.cpp          # FreeRTOS 任务创建、消息队列(Queue)的总线分发，集成 gui_bridge
│
├── components/                   # 【团队协作的地盘】各个独立功能模块
│   │
│   ├── system_services/          # 👉 【基础】系统级公共服务
│   │   ├── CMakeLists.txt
│   │   ├── Kconfig               # Smart Fridge 配置菜单 (WiFi/LLM API 默认值)
│   │   ├── include/
│   │   │   ├── wifi_manager.h    # WiFi 连接接口
│   │   │   ├── sd_storage.h      # SD 卡初始化接口
│   │   │   └── credentials_manager.h  # NVS 凭证管理接口
│   │   ├── wifi_manager.c        # ESP-Hosted WiFi 连接 (通过 C6 协处理器)
│   │   ├── sd_storage.c          # SD 卡 SPI 模式驱动 + FATFS 挂载
│   │   └── credentials_manager.c # WiFi/API Key 的 NVS 存取 (支持 GUI 运行时修改)
│   │
│   ├── gui_port/                 # 👉 【基础】显示/触摸/LVGL 任务底座
│   │   ├── CMakeLists.txt
│   │   ├── include/gui_port.h    # gui_port_init() 接口
│   │   └── gui_port.c            # 基于 BSP 初始化 LCD/Touch/LVGL，定制任务栈与缓冲
│   │
│   ├── gui_bridge/               # 👉 【GUI】系统事件 → LVGL 的抽象桥接层
│   │   ├── CMakeLists.txt
│   │   ├── include/gui_bridge.h  # 屏幕控制、导航、库存刷新、通知等抽象接口
│   │   └── gui_bridge.c          # 已接入：通过 lv_async_call 把后台事件安全派发到 LVGL 线程
│   │
│   ├── gui_app/                  # 👉 【GUI】LVGL 业务界面层 (Launcher + 各应用页)
│   │   ├── CMakeLists.txt
│   │   ├── include/
│   │   │   ├── gui_app.h         # 屏幕切换管理器 & 各应用页对外接口
│   │   │   ├── gui_theme.h       # 全局主题 token、字体与样式声明
│   │   │   ├── gui_styles.h      # 兼容旧代码的主题别名
│   │   │   ├── gui_components.h  # 通用卡片、标签、按钮、弹窗组件
│   │   │   └── gui_icons.h       # Material Icons 文本图标常量
│   │   ├── gui_app.cpp           # 初始化、导航栈、屏幕切换、FreeType 字体加载
│   │   ├── gui_theme.cpp         # TTF 字体加载到 PSRAM + 全局样式初始化
│   │   ├── gui_components.cpp    # 可复用控件工厂
│   │   ├── gui_icons.cpp         # 图标字体声明
│   │   ├── gui_launcher.cpp      # 桌面启动器 (状态栏 + 应用网格 + 拍照存取入口)
│   │   ├── gui_standby.cpp       # 待机/锁屏界面
│   │   ├── gui_overlays.cpp      # 全局浮层 (通知、语音指示器、TTS 文本、相机占位)
│   │   ├── gui_voice_assist.cpp  # 全屏语音助手模态、波形动画、对话气泡
│   │   ├── app_inventory.cpp     # 库存管理页 (侧边栏筛选 + 食材卡片 + 详情弹窗)
│   │   ├── app_recipes.cpp       # 智能食谱页 (食谱卡片 + 详情弹窗)
│   │   └── app_settings.cpp      # 系统设置页 (WiFi/显示/声音/语音/时间日期/设备信息/关于)
│   │
│   ├── system_manager/           # 👉 【基础】运行时系统管理器 (WiFi/显示/音量/设备信息)
│   │   ├── CMakeLists.txt
│   │   ├── include/system_manager.hpp  # SystemManager 静态类接口
│   │   └── system_manager.cpp    # WiFi 扫描与连接、屏幕亮度、音量、设备信息，NVS 持久化设置
│   │
│   ├── vision_hal/               # 👉 【A】图像与视觉模块 (待 A 实现，当前工程中尚未创建)
│   │
│   ├── audio_hal/                # 👉 【B】语音与音频模块
│   │   ├── CMakeLists.txt
│   │   ├── include/audio_api.h   # C 接口：初始化、监听、TTS、实时语音、浏览器 PCM
│   │   ├── audio_hal.cpp         # ES7210/ES8311、采集/播放 ring、ASR/TTS 任务编排
│   │   ├── asr_client.cpp        # 火山流式 ASR WebSocket 协议
│   │   ├── tts_client.cpp        # 火山双向 TTS WebSocket 协议
│   │   ├── realtime_client.cpp   # 端到端实时语音 WebSocket 协议
│   │   ├── volc_binary_protocol.cpp
│   │   └── websocket_diagnostics.cpp # WebSocket/TLS 错误诊断提示
│   │
│   ├── inventory_system/         # 👉 【C1】本地食材数据库
│   │   ├── CMakeLists.txt
│   │   ├── include/
│   │   │   ├── inventory.hpp             # IngredientType/IngredientBatch 数据模型与 CRUD 接口
│   │   │   ├── inventory_history.hpp     # 历史记录与消耗速率计算接口
│   │   │   └── recipe_matcher.hpp        # 食谱结构与匹配算法接口
│   │   ├── inventory_manager.cpp # 核心增删改查、FIFO 批次扣减、过期检测、互斥锁保护
│   │   ├── inventory_history.cpp # 存取历史记录、消耗速率统计 (支持 LLM Prompt 注入)
│   │   ├── recipe_matcher.cpp    # 本地食谱库 + 食谱匹配算法 (完全可做 / 差 N 样)
│   │   └── category_table.c      # 11 类食材分类表与默认保质期查询
│   │
│   ├── ai_agent/                 # 👉 【C2】云端大模型中枢
│   │   ├── CMakeLists.txt
│   │   ├── include/ai_agent.hpp
│   │   ├── llm_client.cpp        # HTTP/SSE Client，调用云端大模型 API
│   │   ├── prompt_builder.cpp    # 组装 System Prompt (含库存/消耗/食谱上下文)
│   │   ├── ai_worker.cpp         # 异步 LLM worker + 流式分句 TTS
│   │   ├── response_parser.cpp   # 解析业务 JSON action
│   │   ├── response_executor.cpp # 执行/确认库存动作
│   │   └── llm_context.cpp       # Prompt 上下文缓存 + 对话历史
│   │
│   ├── web_panel/                # 👉 【C3】Web 面板后台
│   │   ├── CMakeLists.txt
│   │   ├── include/web_panel.h
│   │   ├── web_server.cpp        # HTTP/HTTPS Server + 前端静态资源/证书内嵌
│   │   └── api_routes.cpp        # REST API + WebSocket 文本/二进制语音通道
│   │
│   ├── dashboard/                # 👉 【C5】桌面看板（留言板 + 天气）
│   │   ├── CMakeLists.txt
│   │   ├── include/
│   │   │   ├── dashboard.hpp     # 后台定时刷新任务接口 + 回调注册
│   │   │   ├── notes_board.hpp   # 留言板数据结构与 CRUD 接口
│   │   │   └── weather.hpp       # 天气缓存结构与刷新接口
│   │   ├── dashboard.cpp         # 每 30 分钟天气刷新任务，成功后回调通知 GUI
│   │   ├── notes_board.cpp       # 留言板：内存缓存 + SD 卡 JSON 持久化 + 互斥锁，最多 10 条
│   │   └── weather.cpp           # 天气：HTTPS 拉取 + cJSON 字段路径解析 + 缓存
│   │
│   └── env_sensors/              # 👉 【C4】环境与低功耗控制
│       ├── CMakeLists.txt
│       ├── include/rtc_time.h
│       └── rtc_time.c            # NTP 对时、手动设时与北京时间设置；PIR/低功耗待新建组件
│
├── internal_fs_data/             # 打包进 storage 分区的文件系统资源 (fatfs_create_spiflash_image)
│   ├── NotoSansSC-Regular.ttf    # FreeType 动态加载的中文字体
│   └── MaterialIcons-Regular.ttf # Material Icons 图标字体
│
└── web_frontend/                 # 手机 Web UI 的前端源码
    ├── index.html
    ├── style.css
    └── app.js
```

> [!NOTE]
> **关于视觉/语音模块**：`vision_hal`（A）尚未创建，拍照识别仍是当前最大缺口。`audio_hal`（B）已经创建并接入 ES7210/ES8311、云端 ASR、TTS、端到端实时语音和浏览器 PCM；但本地唤醒词检测仍是占位任务，板载免触发唤醒还未完成。

***

## 二、 文件说明

### 1 `partitions.csv` (分区表)

基于 32MB Nor Flash 优化的自定义分区表，充分利用存储空间：

```
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     ,        0x6000,
otadata,  data, ota,     ,        0x2000,
phy_init, data, phy,     ,        0x1000,
factory,  app,  factory, ,        4M,
model,    data, fat,     ,        3M,
storage,  data, fat,     ,        12M,
```

| 分区        | 大小   | 用途                           |
| --------- | ---- | ---------------------------- |
| `nvs`     | 24KB | 系统 NVS 存储（含 WiFi/API Key 凭证） |
| `factory` | 4MB  | 应用代码区                        |
| `model`   | 3MB  | 本地 AI 图像分类模型（只读，可独立刷写）       |
| `storage` | 12MB | FAT 文件系统（TTS 音素库、食谱数据、备用存储）  |

### 2. main/ (主程序入口与系统总调度)

#### 2.1 `main/system_events.h`  (通信数据结构)

这是整个团队的**通信契约**，定义全局事件枚举和结构体，每个人都可以 include 这个头文件。

- **`sys_event_type_t`** **枚举**：定义了所有可能的全局事件（如语音指令收到、图像识别完毕、触发休眠等）。
- **Payload 结构体**：当事件发生时，光有类型往往不够，还需要附带数据。因此定义了像 `voice_cmd_payload_t` 和 `vision_infer_payload_t` 这样的结构体。
- **通用外壳** **`sys_event_t`**：使用 `void* payload` 指针，队列中每个元素大小固定且极小。

#### 2.2 `main/task_manager.cpp`

系统的**中央调度室**，包括 FreeRTOS 任务创建、消息队列的总线分发，已集成 `gui_bridge` 桥接调用。

- **事件队列容量**：40 个事件，栈空间 8192 字节
- **`send_system_event`**：全局发送接口，发送方用 `malloc` 分配 payload，总线使用完后自动 `free`
- **`system_bus_task`**：事件大循环 `switch-case`，收到事件后调用对应的业务逻辑和 `gui_bridge` 接口；当前还承担语音会话状态机、WebSocket 语音状态广播、流式 TTS 控制等职责

### 3. `components/system_services/` (系统级公共服务)

将 WiFi 连接、SD 卡驱动、凭证管理等**系统级基础设施**从各业务模块中抽取出来，避免组件间的不必要耦合。

- **`credentials_manager.c`**：基于 NVS 的凭证管理器
  - 首次启动时使用 Kconfig 配置的默认值
  - 运行时可通过 `credentials_set_*()` 接口修改（GUI 和 Web 设置页面使用）
  - 所有修改自动持久化到 NVS
  - 管理的凭证：WiFi SSID/密码、天气 API、ASR/LLM/TTS/Realtime 四类语音模型配置
  - 语音模型配置支持当前值 + 最多 5 条历史；Key 脱敏占位时保留原值，避免误清空
- **`Kconfig`**：在 `idf.py menuconfig` 中提供 "Smart Fridge Configuration" 菜单
  - WiFi Settings：默认 SSID 和密码
  - Voice Model Settings：ASR / LLM / TTS / Realtime 默认 URL、Key、模型和扩展 JSON
  - Weather API Settings：天气 URL 模板、Key、位置和 JSON 字段路径

### 3.1 `components/gui_port/` (显示/触摸/LVGL 任务底座)

GUI 的最底层硬件适配。介于 BSP（`waveshare__esp32_p4_wifi6_touch_lcd_7b`）与业务 UI 之间，封装显示与输入初始化、LVGL 任务调度，是 `gui_app` 与 `gui_bridge` 的运行前提。

- **`gui_port.c`**：调用 `bsp_display_start_with_config()` 完成 LCD 初始化、触摸驱动挂载与 LVGL timer task 启动。关键定制：
  - 将 LVGL 任务栈从默认的 7KB 提升到 20KB，以满足 FreeType 中文渲染和设置页 NVS 写入的栈需求。
  - 启用软件旋转并固定为 180 度，规避 BSP 硬件旋转限制。
  - 启动时保持背光关闭，先绘制英文内置字体 boot frame；等 CJK 字体加载和 Launcher 构建完成后再调用 `gui_port_brightness_fade_in()` 渐亮，减少 DSI 未初始化帧/撕裂闪屏。

### 3.2 `components/gui_port/` 的上层 `components/gui_app/` (LVGL 业务界面层)

基于 LVGL 实现《UI 设计方案》中的 Launcher + 独立应用模块架构，是 `gui_bridge` 抽象接口的具体实现承载层。详见各应用页文件说明：

- **`gui_app.cpp`**：屏幕切换管理器。维护导航栈（`gui_app_id_t` 数组），实现 `gui_app_init()`（Splash → Launcher）、`gui_app_navigate_to(app_id)`（横向滑入动画，按需创建/销毁屏幕）、`gui_app_go_back()`、待机/唤醒。
- **`gui_theme.cpp`**：负责 FreeType 字体加载——将 `/internal/NotoSansSC-Regular.ttf` 和 `/internal/MaterialIcons-Regular.ttf` 缓存到 PSRAM，加小块读取、`esp_task_wdt_reset()` 和 tick yield，降低启动期 WDT 风险。
- **`gui_launcher.cpp`**：桌面启动器。顶部状态栏（设备名 / WiFi 图标 / 实时时钟）、左侧核心信息区（天气卡片 + 留言板）、右侧应用网格、底部「拍照存取」横幅入口。
- **`gui_standby.cpp`**：待机/锁屏界面，黑底白字动态时钟和日期，点击任意位置唤醒。
- **`gui_overlays.cpp`**：全局浮层，挂在 `lv_layer_top()` 上——语音监听指示器、通知弹窗（滑入动画 + 3 秒自动隐藏）、TTS 文本悬浮条（绿色，5 秒自动隐藏）、相机预览占位层。
- **`gui_voice_assist.cpp`**：全屏语音助手模态，支持关闭、打断、级联/实时模式切换、波形动画、状态文案和用户/助手消息气泡。
- **`app_inventory.cpp`**：库存管理页。侧边栏筛选（全部 / 临期预警 / 水果 / 蔬菜 / 其他）+ Flex 流式食材卡片，支持卡片详情、单个加减、清空二次确认。提供 `app_inventory_refresh()`。
- **`app_recipes.cpp`**：智能食谱页。侧边栏（推荐 / 全部）+ 食谱卡片，支持食谱详情弹窗、缺失食材标记和跳转库存。提供 `app_recipes_refresh()`。
- **`app_settings.cpp`**：系统设置页。侧边栏包含 WiFi 设置（扫描列表 + 密码输入 + 连接状态轮询）、显示设置（亮度滑块）、声音设置（音量滑块）、语音模型概览、时间日期设置（状态查看、手动设时、立即 NTP 同步）、设备信息和关于。WiFi 扫描与连接通过 `system_manager` 完成。

### 3.3 `components/system_manager/` (运行时系统管理器)

封装运行时可动态控制的系统资源，作为 GUI 设置页与底层 BSP/NVS 之间的桥梁，供 `gui_app` 调用。

- **`system_manager.cpp`**：`SystemManager` 静态类，职责包括：
  - **WiFi**：`scan_wifi_async()` 异步扫描、`connect_wifi(ssid, pass)` 连接（根据扫描结果动态选择认证阈值，支持 OPEN/WPA/WPA2）、状态机管理（含断线自动重连 5 次）、IP/SSID 查询。启动时调用 `wifi_manager_disable_retry()` 接管 `wifi_manager.c` 的重试逻辑。
  - **显示**：`set_brightness(percent)` 通过 BSP PWM 调节背光，NVS 持久化。
  - **声音**：`set_volume(percent)` 当前主要持久化设置；`main` 初始化音频后会调用 `audio_hal_set_output_volume()` 应用保存值，设置页运行时实时改硬件音量仍需补齐。
  - **设备信息**：`get_firmware_version()` / `get_free_heap()` / `get_total_heap()`。
  - 事件回调更新 WiFi 图标状态（通过 `gui_app_set_wifi_status()`），并在真正获得 IP 后通过网络状态回调通知 `main` 补启动 Web 面板和触发时间同步。

### 4. `components/gui_bridge/` (GUI 事件桥接层)

系统事件与 GUI 框架之间的**抽象桥接层**。已通过 LVGL 接入真实实现：所有接口内部使用 `lv_async_call()` 将后台线程产生的事件安全派发到 LVGL 主渲染线程执行，避免跨 Task 死锁。底层调用 `gui_app` 提供的具体 LVGL 控件操作。

接口包括（见 `gui_bridge.h`）：

- `gui_bridge_wake()` / `gui_bridge_show_standby()`：屏幕亮灭控制（亮屏恢复背光 + 退出待机页；待机关闭背光并切换待机界面）
- `gui_bridge_navigate_to(app_id)` / `gui_bridge_go_back()`：应用页导航
- `gui_bridge_refresh_inventory()`：刷新当前可见的业务页内容（库存页 / 食谱页）
- `gui_bridge_show_notification(title, message)`：顶部通知弹窗（带滑入动画，3 秒自动消失）
- `gui_bridge_show_listening_indicator(bool)`：语音监听状态指示
- `gui_bridge_show_tts_text(text)`：TTS 文本显示（底部绿色悬浮条，5 秒后自动隐藏）
- `gui_bridge_show_camera_preview()` / `gui_bridge_show_voice_assist()`：拍照/语音全屏模态
- `gui_bridge_voice_set_state()` / `gui_bridge_voice_add_message()`：全屏语音助手状态和对话内容刷新

> 注：桥接层为纯抽象层，不直接持有 LVGL 对象。具体 UI 行为由 `components/gui_app/` 实现，硬件背光/显示控制由 BSP 完成。

### 5. `components/vision_hal/` (图像与视觉模块) 【A】

> **当前状态：待 A 模块接入，工程目录中尚未创建。**

负责摄像头硬件驱动和本地图像推断，是食材识别功能的核心。系统总线已预留 `EVT_VISION_INFER_DONE` 事件及其 `vision_infer_payload_t`（含 `top1_id` 与 `confidence`）挂载点，A 接入后只需投递事件即可被 `task_manager` 路由处理。

### 6. `components/audio_hal/` (语音与音频模块) 【B】

> **当前状态：已创建并接入云端 ASR/TTS/实时语音；本地唤醒词检测待实现。**

负责 ES7210 麦克风采集、ES8311 扬声器播放、云端语音识别、TTS 合成、端到端实时语音和浏览器外部 PCM 输入。核心能力：

- **`audio_hal.cpp`**：统一 C API，管理 16 kHz/16-bit PCM 采集、外部浏览器 PCM、播放 ring buffer、TTS 队列、打断、级联/实时模式切换和音量应用。
- **`asr_client.cpp`**：火山流式 ASR WebSocket 协议，支持 gzip 帧、partial/final 文本、错误诊断和旧版/新版鉴权配置。
- **`tts_client.cpp`**：火山双向 TTS WebSocket 协议，流式接收 PCM 并送入扬声器。
- **`realtime_client.cpp`**：端到端实时语音 WebSocket 协议，支持 ASR 文本、对话文本、24 kHz 音频下采样播放和打断。
- **`audio_hal_start_wake_word()`**：当前只启动占位 `wake_word_task`，尚未接入实际唤醒词检测模型/算法。
- **总线联动**：`task_manager` 会把 ASR 结果转为 `EVT_VOICE_CMD_RCVD`，把 LLM 分句输出转为 `EVT_TTS_STREAM_*`，并在 TTS 完成后继续聆听或回到 idle。

### 7. `components/inventory_system/` (本地食材数据库) 【C1】

本地食材库的核心，负责食材数据的增删改查与智能分析。数据持久化依赖 `system_services` 提供的 SD 卡服务；SD 卡不可用时自动降级为纯内存模式。

- **`inventory_manager.cpp`**：
  - 以内存缓存 + SD 卡 JSON 文件作为双层存储（当前方案，后续可迁移至 SQLite）。
  - **数据模型**：采用 `IngredientType`（食材种类）+ `IngredientBatch`（单次存入批次）分离的批次管理架构，支持 FIFO 批次存取。
  - **CRUD 接口**：`add_ingredient`（创建新批次）/ `remove_ingredient`（按 FIFO 优先扣减最早批次）/ `get_all_ingredients` / `clear_ingredient` / `update_ingredient`，每次写操作同步持久化至 `/sdcard/inventory.json`，全程由互斥锁保护并发访问。
  - **过期检测**：`check_expiring_ingredients(threshold_days)` 基于各批次的 `expire_time` 时间戳判断临期/已过期食材。
- **`inventory_history.cpp`**：食材存取历史记录与消耗速率统计。保留 90 天，`compute_all_consumption_rates(days)` 计算日均消耗量，供 `ai_agent` 注入 LLM Prompt。
- **`recipe_matcher.cpp`**：本地食谱库 + 匹配算法。首次运行写入内置默认食谱集到 `/sdcard/recipes.json`，支持 `recipe_match_available()`（完全可做）/ `recipe_match_near(max_missing)`（差 N 样以内）匹配，按覆盖率排序。
- **`category_table.c`**：11 类食材分类表（水果/蔬菜/肉禽/海鲜/蛋奶/豆制品/主食/调味品/饮品/零食/冻品/其他），提供 `category_lookup()` 与默认保质期查询。
- **`include/`**：`inventory.hpp`（数据模型与 CRUD 接口）、`inventory_history.hpp`、`recipe_matcher.hpp`。

### 8. `components/ai_agent/` (云端大模型中枢) 【C2】

负责与云端大模型通信，实现复杂语音指令理解和菜谱推荐等需要 AI 能力的功能。API 凭证由 `system_services` 的 `credentials_manager` 统一管理。

- **`llm_client.cpp`**：基于 `esp_http_client` 封装的 HTTPS 客户端，支持普通 JSON 响应和 SSE 流式响应。API URL、Key 和模型名称均在运行时从 NVS 动态读取。
- **`prompt_builder.cpp`**：
  - `build_llm_request()`：将当前冰箱库存、近 7 天消耗速率、可做菜品推荐注入 System Prompt，与用户文本组装为 OpenAI 兼容格式的完整请求体。模型名称通过 `credentials_get_llm_model()` 动态获取。
- **`ai_worker.cpp`**：异步 worker 任务，避免 LLM 请求阻塞系统总线；支持取消、队列清空、本地解析“放入/取出”简单库存指令、SSE delta 分句转 TTS。
- **`response_parser.cpp`**：解析 OpenAI 兼容外层响应和内层业务 JSON，支持 `ADD` / `REMOVE` / `BATCH` / `RECIPE` / `CHAT`。
- **`response_executor.cpp`**：库存写操作先保存为 60 秒待确认动作，用户说“确认/取消”后再执行；执行成功后刷新库存上下文并投递 GUI 更新。
- **`llm_context.cpp`**：缓存库存、消耗速率、可做菜品和最近对话，降低每次 prompt 构建成本。

### 9. `components/web_panel/` (Web 控制面板) 【C3】

提供轻量级手机 Web 管理界面，作为原生 App 的替代方案。手机与开发板连接同一 WiFi 后，通过浏览器访问即可使用。

- **`web_server.cpp`**：基于 `esp_http_server` 启动 HTTP 管理服务，同时尝试用 `esp_https_server` 启动 HTTPS 语音服务；前端静态资源和自签证书通过 `_binary_*_start` 符号内嵌编译进固件。启动函数可重复调用，开机离线时会在后续 WiFi 获得 IP 后补启动。
- **`api_routes.cpp`**：实现 RESTful API 路由，包括 `GET/POST /api/inventory`（库存查询/录入取出，写操作需要 `confirmed=true`）、`GET /api/history`、`GET /api/status`、`GET/POST /api/settings`、`GET/POST /api/time`、`POST /api/time/sync`、`POST /api/chat`、`GET /api/recipes/match`、`POST /api/recipes`、`GET/POST /api/notes`、`GET/POST /api/weather`、`POST /api/weather/refresh`、`GET/POST /api/voice/config`、`POST /api/voice/config/delete`、`POST /api/voice/test-tts`，以及 `/ws` WebSocket 端点。
- **WebSocket**：文本消息用于语音会话控制、状态同步和聊天输入；二进制消息用于浏览器麦克风 16 kHz PCM 上传，音频输出 PCM 也可广播给前端播放。

### 9.1 `components/dashboard/` (桌面看板：留言板 + 天气) 【C5】

承载 Launcher 桌面左侧"天气卡片"与"留言板"的后端逻辑，让两者从硬编码假数据变为真实可管理的数据。

- **`notes_board.cpp`**：留言板。内存缓存 `std::vector<Note>` + 互斥锁 + SD 卡 JSON 持久化（`/sdcard/notes.json`），最多保留 10 条、超出按 FIFO 丢弃。SD 卡不可用时降级纯内存模式。接口：`notes_init/get_all/add/delete/clear`。
- **`weather.cpp`**：天气。从 `credentials_manager` 读取用户配置的天气 API（URL/Key/位置/字段路径），通过 `esp_http_client`（HTTPS + crt_bundle）拉取，用** cJSON 字段路径遍历器**（按 `.` 分隔，如 `now.temp`）提取温度与天气描述，适配和风/心知等不同服务商的 JSON 结构。接口：`weather_init/get/refresh`。
- **`dashboard.cpp`**：后台任务，每 30 分钟（仅 `rtc_time_is_synced()` 时）调用 `weather_refresh()`，成功后通过 `dashboard_set_refresh_callback()` 注册的回调（`main` 设为 `gui_bridge_refresh_dashboard`）通知 GUI 刷新桌面。采用回调注入而非直接依赖 `gui_bridge`，避免组件循环依赖。
- **配置存储**：天气 API 凭证复用 `credentials_manager` 的 NVS key-value 模式（`wx_url/wx_key/wx_city/wx_location/wx_temp_path/wx_text_path`），支持 Web 设置页运行时修改。

### 10. `components/env_sensors/` (环境监测与低功耗) 【C4】

负责外部传感器驱动和系统功耗管理，提升设备使用体验和续航。

- **`rtc_time.c`**：联网时通过 SNTP 服务同步网络时间（北京时间 CST-8），支持连接 WiFi 后补同步、Web/GUI 手动同步和手动设置系统时间。NTP 超时不致命——若 RTC 电池维持了有效时间（时间戳 > 2023）则视为已同步；`rtc_time_is_synced()` 供临期检查等模块判断时间可信度。
- **PIR/低功耗**：> **待实现。** 当前工程没有 `power_manage.c` 或等价组件。事件总线与 `gui_bridge` 侧（`EVT_PIR_TRIGGERED` → `gui_bridge_wake`，`EVT_GOTO_SLEEP` → `gui_bridge_show_standby`）已就绪，但 GPIO 中断、空闲计时和 Light Sleep 策略尚未落地。

### 11. `web_frontend/` (手机 Web 前端)&#x20;

手机 Web 面板的前端源码，**直接内嵌编译至固件中**，跟随 app 固件一起烧录更新，不占用 SD 卡存储。

- **`index.html`**：单页应用主页面，包含库存列表、录入/取出操作界面和系统设置入口；设置页包含 WiFi、天气和时间日期配置。
- **`style.css`**：响应式样式，适配手机浏览器。
- **`app.js`**：前端逻辑，通过 Fetch API 调用后端 RESTful 接口，通过 WebSocket 接收实时推送通知。

### 12. `internal_fs_data/` (内部文件系统资源)

通过 `main/CMakeLists.txt` 中的 `fatfs_create_spiflash_image(storage ../internal_fs_data FLASH_IN_PROJECT)` 打包进 `storage` 分区（FAT），编译/烧录时自动生成镜像。运行时挂载于 `/internal`。

- **`NotoSansSC-Regular.ttf`**：中文字体，由 `gui_theme.cpp` 通过 `lv_freetype` 在运行时动态加载，并缓存到 PSRAM 以加速字形渲染。
- **`MaterialIcons-Regular.ttf`**：Material Icons 图标字体，由 `gui_theme.cpp` 加载为 `font_icon_24/36`。
